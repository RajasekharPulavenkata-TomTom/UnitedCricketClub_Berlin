# Install with: pip install -r requirements-test.txt
# Then for E2E: playwright install chromium

pytest==9.0.3
pytest-playwright==0.9.0  # must track pytest major: 0.5.2 caps pytest<9, breaks with pytest 9
