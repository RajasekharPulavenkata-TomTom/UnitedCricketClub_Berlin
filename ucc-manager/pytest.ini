[pytest]
testpaths = tests
addopts = -v --tb=short
markers =
    api: backend API tests (require TEST_DATABASE_URL)
    e2e: end-to-end browser tests (require E2E_BASE_URL + E2E_USERNAME + E2E_PASSWORD)
